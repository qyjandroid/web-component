import React from 'react';
import { {{pascalCase name}}Props } from './types';
import clsx from "clsx";
import { createNamespace } from '../../utils/create';
import { BEM } from '../../utils/create/bem';
import './style/index.less';


class {{pascalCase name}} extends React.Component<{{pascalCase name}}Props> {

    static defaultProps = {
        className:""
    };

    private bem: BEM = createNamespace('{{kebabCase name}}');

    constructor(props: {{pascalCase name}}Props) {
        super(props);
    }

    render() {
        const {className}=this.props;
        const { bem } = this;
        return (
            <div className={clsx(className, bem())}>
                {{pascalCase name}}
            </div>
        );
    }
}

export default {{pascalCase name}};